<h1>New Advertisement</h1>

<div class="row">
	<div class="col-md-4">
		<p>Guidelines for Advertisements</p>
		<ul>
			<li>Make sure it rhymes.</li>
			<li>Don't use the letter "A".</li>
		</ul>
	</div>
	<div class="col-md-8">
		<%= form_for @advertisement do |f| %>
			<div class="form-group">
				<%= f.label :copy %>
				<%= f.text_field :title, class: 'form-control', placeholder: "Enter Advertisement title" %>
			</div>
			<div class="form-group">
				<%= f.label :copy %>
				<%= f.text_area :copy, rows: 8, class: 'form-control', placeholder: "Enter Advertisement copy" %>
			</div>
			<div class="form-group">
				<%= f.label :price %>
				<%= f.text_field :price, class: 'form-control', placeholder: "Enter Advertisement price" %>
			</div>
			<div class="form-group">
				<%= f.submit "save", class: 'btn btn-success' %>
			</div>
		<% end %>
	</div>
</div>